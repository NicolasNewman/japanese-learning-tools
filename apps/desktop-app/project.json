{
    "name": "desktop-app",
    "projectType": "application",
    "sourceRoot": "apps/desktop-app/src",
    "targets": {
        "build": {
            "executor": "nx:run-commands",
            "outputs": [
                "{projectRoot}/build"
            ],
            "options": {
                "command": "cd {projectRoot} && pnpm tauri build",
                "cwd": "{workspaceRoot}"
            }
        },
        "dev": {
            "executor": "nx:run-commands",
            "options": {
                "command": "pnpm tauri dev",
                "cwd": "{workspaceRoot}"
            }
        }
    }
}