# gd-sudachi

A python tool for converting HTML text to HTML supported by the SRS' highlighting feature of subs2clipboard

## Environment Setup

The following packages are required for building locally:
```sh
sudo apt-get install patchelf
```

To test locally:
```sh
poetry run python gd_sudachi/run.py --help
```